<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Accent -->
    <color name="accent">@android:color/holo_blue_light</color>

    <!-- Background -->
    <color name="primary_default">@*android:color/background_material_dark</color>                  <!-- Primary -->
    <color name="background_default">@*android:color/background_material_dark</color>               <!-- Main -->
    <color name="dialog_background_default">@*android:color/background_material_dark</color>        <!-- Dialog -->
    <color name="snackbar_background_default">@*android:color/background_material_dark</color>      <!-- SnackBar -->
    <color name="splash_background">@*android:color/background_material_dark</color>                <!-- SplashScreen -->
    <color name="navbar_bg_default">@*android:color/background_material_dark</color>                <!-- NavBar -->
    <color name="drawer_background_default">@*android:color/background_material_dark</color>        <!-- Drawer -->
    <color name="drawer_selected_default">#1a000000</color>                                         <!-- Drawer Selected -->
    <color name="md_grey_900">#1a000000</color>                                                     <!-- Switch Track -->
</resources>