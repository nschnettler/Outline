<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Notifications - Quick Reply Background -->
    <style name="systemui_theme_remote_input" parent="@*android:style/Theme.DeviceDefault">
        <item name="android:colorAccent">@*android:color/accent_material_dark</item>
    </style>

    <!-- Dialogs -->
    <style name="Theme.SystemUI.Dialog.Alert" parent="@*android:style/Theme.DeviceDefault.Dialog.Alert" />  <!-- ABD Confirmation Dialog, ... -->
    <style name="Theme.SystemUI.Dialog" parent="@android:style/Theme.DeviceDefault.Dialog" />
</resources>